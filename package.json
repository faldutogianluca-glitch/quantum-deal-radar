{
  "name": "quantum-deal-radar",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @qdr/core -w @qdr/scrapers -w @qdr/server",
    "test": "npm run test -w @qdr/core -w @qdr/scrapers -w @qdr/server",
    "scrape": "npm run build && node packages/server/dist/cli.js scrape",
    "enrich": "npm run build && node packages/server/dist/cli.js enrich",
    "serve": "npm run build && node packages/server/dist/cli.js serve",
    "watch": "npm run build && node packages/server/dist/cli.js watch",
    "verifica": "npm run build && node packages/server/dist/cli.js verifica",
    "cattura": "npm run build && node packages/server/dist/cli.js cattura",
    "ispeziona": "npm run build && node packages/server/dist/cli.js ispeziona",
    "pdftesto": "npm run build && node packages/server/dist/cli.js pdftesto",
    "calibra": "npm run build && node packages/server/dist/cli.js calibra",
    "novita": "npm run build && node packages/server/dist/cli.js novita"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
