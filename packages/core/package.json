{
  "name": "@qdr/core",
  "version": "0.2.0",
  "type": "module",
  "description": "Dedup cross-fonte, risoluzione zone OMI e triangolazione valutativa per Quantum Deal Radar. Zero dipendenze.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^26.1.2"
  }
}
